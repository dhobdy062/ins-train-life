export const mutation = (config) => config;
export const query = (config) => config;
export const action = (config) => config;
export const internalMutation = (config) => config;
export const internalAction = (config) => config;
